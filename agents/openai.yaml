interface:
  display_name: "/xxd-panel-049"
  short_description: "Create Panel 049 from its original style brief."
  default_prompt: "Use $xxd-panel-049; treat its bundled original style brief as the sole aesthetic authority, and ask only for unresolved mode, size, text mode, locale, or wallpaper settings."

policy:
  allow_implicit_invocation: true
