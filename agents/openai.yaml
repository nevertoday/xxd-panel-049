interface:
  display_name: "/xxd-panel-049"
  short_description: "Create source-bound fresh woodcut artwork"
  default_prompt: "Use $xxd-panel-049 with my photograph, ask for modes and copy settings, then create fresh raster outputs."
policy:
  allow_implicit_invocation: true
